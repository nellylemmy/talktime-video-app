# TalkTime Production Environment Variables
NODE_ENV=production

# Database Configuration
DB_PASSWORD=talktime_secure_pass_2024
DB_HOST=db
DB_USER=talktime_user
DB_DATABASE=talktimedb
DB_PORT=5432

# Redis Configuration
REDIS_URL=redis://redis:6379

# Security
SESSION_SECRET=change_this_to_random_string_in_production
JWT_SECRET=change_this_jwt_secret_in_production

# Application URLs
FRONTEND_URL=http://your-vps-ip:8101
BACKEND_URL=http://localhost:3001
CORS_ORIGINS=http://your-vps-ip:8101

# Email Configuration (Optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Push Notifications
VAPID_SUBJECT=mailto:support@talktime.app
NOTIFICATION_PERSISTENCE_ENABLED=true
NOTIFICATION_ANALYTICS_ENABLED=true

# WebRTC — Self-hosted coturn TURN server
# Generate secret: openssl rand -hex 32
COTURN_SECRET=your_64_char_hex_secret_here
TURN_HOST=62.72.3.138
TURN_PORT=3478

# Resource Limits
MAX_PARTICIPANTS=50
VIDEO_QUALITY=HD
ENABLE_RECORDING=false

# Optional: Mailchimp Integration (for newsletter widget)
# If not configured, newsletter features will be disabled gracefully
# MAILCHIMP_API_KEY=your_mailchimp_api_key_here
# MAILCHIMP_SERVER_PREFIX=us1